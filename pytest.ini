[pytest]
# The modules under test live at the repo root, so the root must be importable.
# Without this only `python -m pytest` worked (it adds the CWD); the bare
# `pytest -q` the README documents failed at collection.
pythonpath = .
testpaths = tests
